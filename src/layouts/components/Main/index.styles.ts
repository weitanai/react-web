import styled from 'styled-components';
import { Layout } from 'antd';

export const StyleLayoutContent = styled(Layout.Content)`
  overflow: hidden auto;
  flex: 1 1 auto;
  position: relative;
  background: #f5f5f7;
`;
